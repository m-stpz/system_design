# Hash Tables

- A hash table is a data structure that allows fast lookup regardless of the data size
- For this reason, it's widely used in:
  - db indexing
  - caching
  - program compilation
  - error checking

## Hashing algorithm/function

- Calculation applied to a key
